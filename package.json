{
  "name": "ark",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "serve": "vitepress serve docs",
    "commit": "git add -A && git commit -m 'commit' && git push -f"
  },
  "devDependencies": {
    "vitepress": "^0.20.0"
  }
}
